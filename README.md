# Github API in Javscript

## Quick Start
Make sure you have [Deno](deno.land) installed in your system.
Clone this repo, then
```console
$ deno compile --allow-read --allow-net --allow-env --unstable main.js
$ ./ghjs
```
